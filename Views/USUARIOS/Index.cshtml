@model IEnumerable<ProyectoProgra2.Models.USUARIOS>

@{
    ViewBag.Title = "Usuarios";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Registrar un usuario nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.USUARIO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.USUARIO)
        </td>
        <td>
           @* @Html.ActionLink("Edit", "Edit", new { id=item.USUARIO }) | *@
            @Html.ActionLink("Detalles", "Details", new { id=item.USUARIO }) |
           @* @Html.ActionLink("Delete", "Delete", new { id=item.USUARIO }) *@
        </td>
    </tr>
}

</table>
