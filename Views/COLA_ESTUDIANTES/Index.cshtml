@model IEnumerable<ProyectoProgra2.Models.COLA_ESTUDIANTES>

@{
    ViewBag.Title = "Index";

}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PROCESADO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ESTUDIANTES.CARNET)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ESTUDIANTES.NOMBRE)
        </th>        
        <th>
            @Html.DisplayNameFor(model => model.FECHA_ADICION)
        </th>


        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PROCESADO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ESTUDIANTES.CARNET)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ESTUDIANTES.NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FECHA_ADICION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { CARNET = item.ESTUDIANTES.CARNET, FECHA_ADICION = item.FECHA_ADICION }) |
            @Html.ActionLink("Details", "Details", new { CARNET = item.ESTUDIANTES.CARNET, FECHA_ADICION = item.FECHA_ADICION }) |
            @Html.ActionLink("Delete", "Delete", new { CARNET = item.ESTUDIANTES.CARNET, FECHA_ADICION = item.FECHA_ADICION })

        </td>
    </tr>
}

</table>
