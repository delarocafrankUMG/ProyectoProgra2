@model IEnumerable<ProyectoProgra2.Models.INSCRIPCION_ESTUDIANTES>

@{
ViewBag.Title = "Index";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Inscripcion</title>
</head>
<body>
    <p>
        @Html.ActionLink("Crear Nuevo", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ESTUDIANTES.NOMBRE)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FECHA_ADICION)
            </th>
            <th>

            </th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ESTUDIANTES.NOMBRE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FECHA_ADICION)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new {  CARNET=item.CARNET, FECHA_ADICION = item.FECHA_ADICION }) |*@
                @Html.ActionLink("Detalles", "Details", new { CARNET = item.CARNET, FECHA_ADICION = item.FECHA_ADICION }) |
              @*  @Html.ActionLink("Delete", "Delete", new { CARNET = item.CARNET, FECHA_ADICION = item.FECHA_ADICION })*@
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
